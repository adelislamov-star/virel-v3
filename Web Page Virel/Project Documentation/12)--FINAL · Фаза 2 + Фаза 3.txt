═══════════════════════════════════════════════════════════════════
VIREL V3 — ПОЛНОЕ МАСТЕР ТЗ
Luxury Companion Platform · London · March 2026
Версия: FINAL · Фаза 2 + Фаза 3
═══════════════════════════════════════════════════════════════════

VISION
──────
Virel — это не эскорт сайт. Это luxury companion platform.
Клиент сравнивает нас с The Connaught и NetJets, не с Babylon.
Наш клиент: бизнесмен 35-55 лет, доход £200k+, ценит время,
дискретность и качество. Он бронирует один раз — и остаётся навсегда.

СТЕК (без изменений)
──────────────────────
Next.js 14 · TypeScript · PostgreSQL (Neon) · Prisma
Cloudflare R2 · Vercel · NextAuth · Tailwind
Anthropic Claude API (уже есть ключ)

═══════════════════════════════════════════════════════════════════
ЧАСТЬ 1 — PRISMA SCHEMA
═══════════════════════════════════════════════════════════════════

Прочитай текущий prisma/schema.prisma перед началом.
Добавить следующие модели:

──────────────────────────────────────────────────────────────────
1.1 SERVICE (глобальный справочник сервисов)
──────────────────────────────────────────────────────────────────
model Service {
  id              String   @id @default(cuid())
  name            String   @unique       // "GFE", "CIM", "Body to Body"
  slug            String   @unique       // "gfe", "cim", "body-to-body"
  category        String                 // signature|intimate|wellness|fetish|bespoke
  publicName      String?                // "Girlfriend Experience" (для фронта)
  description     String?                // краткое (для карточки)
  introText       String?                // вступительный абзац SEO страницы
  fullDescription String?                // полный SEO текст /services/[slug]
  seoTitle        String?
  seoDescription  String?
  seoKeywords     String?
  isPublic        Boolean  @default(true)  // false = только Members
  isPopular       Boolean  @default(false)
  isActive        Boolean  @default(true)
  sortOrder       Int      @default(0)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  modelServices   ModelService[]
}

──────────────────────────────────────────────────────────────────
1.2 MODEL SERVICE (сервисы конкретной модели)
──────────────────────────────────────────────────────────────────
model ModelService {
  id         String   @id @default(cuid())
  modelId    String
  serviceId  String
  isExtra    Boolean  @default(false)
  extraPrice Float?                      // CIM +£70, A-Level +£150
  model      Model    @relation(fields: [modelId], references: [id], onDelete: Cascade)
  service    Service  @relation(fields: [serviceId], references: [id])
  @@unique([modelId, serviceId])
}

──────────────────────────────────────────────────────────────────
1.3 CALL RATE MASTER (шаблоны длительностей)
──────────────────────────────────────────────────────────────────
model CallRateMaster {
  id          String      @id @default(cuid())
  label       String      // "30 min", "1 Hour", "Overnight (9 hrs)"
  durationMin Int         // 30, 45, 60, 90, 120, 180, 540, 720
  sortOrder   Int         @default(0)
  isActive    Boolean     @default(true)
  createdAt   DateTime    @default(now())
  modelRates  ModelRate[]
}

──────────────────────────────────────────────────────────────────
1.4 MODEL RATE (тарифы конкретной модели)
──────────────────────────────────────────────────────────────────
model ModelRate {
  id               String         @id @default(cuid())
  modelId          String
  callRateMasterId String
  incallPrice      Float?
  outcallPrice     Float?
  model            Model          @relation(fields: [modelId], references: [id], onDelete: Cascade)
  callRateMaster   CallRateMaster @relation(fields: [callRateMasterId], references: [id])
  @@unique([modelId, callRateMasterId])
}

──────────────────────────────────────────────────────────────────
1.5 DISTRICT (районы Лондона — SEO Level 1)
──────────────────────────────────────────────────────────────────
model District {
  id              String         @id @default(cuid())
  name            String         @unique  // "Mayfair"
  slug            String         @unique  // "mayfair"
  tier            Int            @default(1)  // 1=top, 2=high, 3=medium
  description     String?        // уникальный текст 300-500 слов
  hotels          String[]       // список отелей района
  restaurants     String[]       // список ресторанов района
  landmarks       String[]       // достопримечательности
  seoTitle        String?
  seoDescription  String?
  seoKeywords     String?
  isActive        Boolean        @default(true)
  isPopular       Boolean        @default(false)
  sortOrder       Int            @default(0)
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt
  transportHubs   TransportHub[]
  modelLocations  ModelLocation[]
}

──────────────────────────────────────────────────────────────────
1.6 TRANSPORT HUB (станции метро — SEO Level 2)
──────────────────────────────────────────────────────────────────
model TransportHub {
  id              String   @id @default(cuid())
  name            String   @unique  // "Bond Street"
  slug            String   @unique  // "bond-street"
  districtId      String
  walkingMinutes  Int      @default(5)   // минут пешком от центра района
  description     String?  // 150-250 слов
  seoTitle        String?
  seoDescription  String?
  isActive        Boolean  @default(true)
  createdAt       DateTime @default(now())
  district        District @relation(fields: [districtId], references: [id])
}

──────────────────────────────────────────────────────────────────
1.7 MODEL LOCATION (привязка модели к районам)
──────────────────────────────────────────────────────────────────
model ModelLocation {
  id              String   @id @default(cuid())
  modelId         String
  districtId      String
  isPrimary       Boolean  @default(false)
  transportHubId  String?  // ближайшая станция
  walkingMinutes  Int?     // минут от станции
  model           Model    @relation(fields: [modelId], references: [id], onDelete: Cascade)
  district        District @relation(fields: [districtId], references: [id])
  @@unique([modelId, districtId])
}

──────────────────────────────────────────────────────────────────
1.8 ОБНОВИТЬ MODEL — добавить поля
──────────────────────────────────────────────────────────────────
К существующей Model добавить:

  // Physical Stats
  weight          Int?
  bustSize        String?      // "32B", "34D"
  bustType        String?      // Natural | Enhanced
  eyeColor        String?
  measurements    String?      // "32D-25-33"
  smokingStatus   String?      // Non-Smoker | Occasional | Regular
  tattooStatus    String?      // None | Few | Many
  piercingDetails String[]     // ["Belly", "Nipples"]
  orientation     String?      // Bisexual | Hetero | Lesbian | Other
  languages       String[]     // ["English", "Russian"]
  education       String?      // "Graduate", "Masters"
  travel          String?      // "London only"|"UK & Europe"|"Worldwide"

  // Marketing & Presentation
  tagline         String?      // "Slender Russian Blonde" (Black Label style)
  isExclusive     Boolean      @default(false)
  isVerified      Boolean      @default(false)  // AI verified photos
  availability    String?      // Available Now | Advanced Notice | Away | On Holiday
  duoPartnerIds   String[]     // IDs моделей для DUO
  wardrobe        String[]     // ["Schoolgirl", "Nurse", "Latex"]
  publicTags      String[]     // ["GFE", "Open-minded", "Massage Specialist"]

  // Trust & Safety
  verificationNote  String?    // внутренняя заметка о верификации
  lastActiveAt      DateTime?  // когда последний раз была на связи
  responseTimeMin   Int?       // среднее время ответа агентства в минутах

  // SEO
  seoTitle          String?
  seoDescription    String?

  // Relations
  modelServices   ModelService[]
  modelRates      ModelRate[]
  modelLocations  ModelLocation[]

──────────────────────────────────────────────────────────────────
1.9 CLIENT (клиенты с CRM данными)
──────────────────────────────────────────────────────────────────
model Client {
  // уже существует — добавить поля:
  preferredLanguage String?      // en | ru | ar
  preferredCurrency String?      // GBP | EUR | USD | AED
  tier              String?      // standard | premium | vip
  totalSpend        Float?       // общая сумма бронирований
  lastBookingAt     DateTime?
  internalNotes     String?      // заметки рецепции (не видны клиенту)
  wishlistModelIds  String[]     // избранные модели
  preferredDistricts String[]    // предпочтительные районы
  marketingOptIn    Boolean      @default(false)
  // Telegram
  telegramId        String?
  telegramUsername  String?
}

──────────────────────────────────────────────────────────────────
1.10 SMART MATCH SESSION (AI matching quiz)
──────────────────────────────────────────────────────────────────
model SmartMatchSession {
  id          String   @id @default(cuid())
  sessionId   String   @unique  // anonymous session
  answers     Json     // ответы на вопросы квиза
  resultIds   String[] // IDs рекомендованных моделей
  converted   Boolean  @default(false)  // перешёл ли в бронирование
  createdAt   DateTime @default(now())
}

──────────────────────────────────────────────────────────────────
1.11 AVAILABILITY SLOT (реальная доступность)
──────────────────────────────────────────────────────────────────
model AvailabilitySlot {
  id        String   @id @default(cuid())
  modelId   String
  date      DateTime
  timeFrom  String   // "10:00"
  timeTo    String   // "22:00"
  type      String   // available | busy | away | tentative
  note      String?
  model     Model    @relation(fields: [modelId], references: [id], onDelete: Cascade)
  @@index([modelId, date])
}

──────────────────────────────────────────────────────────────────
1.12 BOOKING REQUEST (онлайн бронирование через агентство)
──────────────────────────────────────────────────────────────────
model BookingRequest {
  id              String   @id @default(cuid())
  // Client info
  clientName      String
  clientEmail     String
  clientPhone     String
  clientTelegramId String?
  preferredContact String  // whatsapp | telegram | phone | email
  // Booking details
  modelId         String?
  model2Id        String?  // для DUO
  districtId      String?
  callType        String   // incall | outcall
  date            DateTime
  duration        Int      // минуты
  address         String?  // для outcall
  hotelName       String?
  roomNumber      String?
  // Pricing
  basePrice       Float
  extrasTotal     Float    @default(0)
  grandTotal      Float
  currency        String   @default("GBP")
  selectedExtras  Json     // [{serviceId, price}]
  // Concierge
  specialRequests String?
  occasion        String?  // birthday | anniversary | business | leisure
  restaurantNeeded Boolean @default(false)
  transportNeeded Boolean  @default(false)
  // Status
  status          String   @default("pending") // pending|confirmed|cancelled
  confirmedAt     DateTime?
  confirmedBy     String?  // userId рецепции
  internalNotes   String?
  source          String   @default("website") // website | phone | whatsapp
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

──────────────────────────────────────────────────────────────────
1.13 CONCIERGE PARTNER (партнёры — рестораны, отели, такси)
──────────────────────────────────────────────────────────────────
model ConciergePartner {
  id          String   @id @default(cuid())
  type        String   // restaurant | hotel | chauffeur | flowers | gifts
  name        String
  districtId  String?
  address     String?
  website     String?
  phone       String?
  description String?
  priceRange  String?  // "££££"
  imageUrl    String?
  isActive    Boolean  @default(true)
  sortOrder   Int      @default(0)
  createdAt   DateTime @default(now())
}

──────────────────────────────────────────────────────────────────
1.14 BLOG POST (editorial content)
──────────────────────────────────────────────────────────────────
model BlogPost {
  id              String   @id @default(cuid())
  title           String
  slug            String   @unique
  excerpt         String?
  content         String   // rich text
  coverImage      String?
  category        String   // guide | lifestyle | london | news
  tags            String[]
  seoTitle        String?
  seoDescription  String?
  publishedAt     DateTime?
  isPublished     Boolean  @default(false)
  authorName      String?
  relatedDistricts String[] // связанные районы для internal linking
  relatedServices String[]  // связанные сервисы
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

После всех изменений: npx prisma db push

═══════════════════════════════════════════════════════════════════
ЧАСТЬ 2 — SEED СПРАВОЧНИКОВ
═══════════════════════════════════════════════════════════════════

Создать scripts/seed-masters.ts

──────────────────────────────────────────────────────────────────
2.1 CallRateMasters (8 шаблонов)
──────────────────────────────────────────────────────────────────
{ label: '30 min',            durationMin: 30,  sortOrder: 1 }
{ label: '45 min',            durationMin: 45,  sortOrder: 2 }
{ label: '1 Hour',            durationMin: 60,  sortOrder: 3 }
{ label: '90 min',            durationMin: 90,  sortOrder: 4 }
{ label: '2 Hours',           durationMin: 120, sortOrder: 5 }
{ label: '3 Hours',           durationMin: 180, sortOrder: 6 }
{ label: 'Overnight (9 hrs)', durationMin: 540, sortOrder: 7 }
{ label: 'Extra Hour',        durationMin: 60,  sortOrder: 8 }

──────────────────────────────────────────────────────────────────
2.2 Services (82 сервиса)
──────────────────────────────────────────────────────────────────

category: "signature" (isPublic: true)
  GFE, FK, DFK, OWO, PSE, DUO, Bi DUO, Couples, Dinner Date,
  Travel Companion, Overnight Experience, Striptease, Lapdancing,
  Roleplay, Shower, Uniform, Dirty Talk, Face Sitting, Fingering,
  Lady's Services, Open minded, Party, Toys, 69, 30 Minute, Massage

category: "intimate" (isPublic: false — только Members)
  CIF, CIM, COB, A-Level, Swallow, Snowballing,
  Rimming giving, Rimming receiving, WS giving, WS receiving,
  Squirting, Fisting, DP, DT, Deep Throat,
  Spanking giving, Soft Spanking receiving, Humiliation,
  Escorts For Girls

category: "wellness" (isPublic: true)
  Body to Body, Nuru, Tantric, Sensual, Erotic,
  Lomilomi, Professional Massage, Prostate Massage

category: "fetish" (isPublic: true)
  BDSM, Domination, Light Domination, Foot Fetish, Strap-on,
  Smoking Fetish, Handcuffs, Tie and Tease, Poppers, Latex,
  Bondage, Rope Bondage, Ball Busting, Caning, Cuckolding,
  Slapping, Trampling, Sensual Wrestling, Fetish

category: "bespoke" (isPublic: false)
  MMF, Group, Filming with mask, Filming WITHOUT mask, Photos With Mask

publicName маппинг (для фронта):
  GFE → "Girlfriend Experience"
  DFK → "Deep French Kiss"
  PSE → "Pornstar Experience"
  DUO → "Duo Available"
  Bi DUO → "Bisexual Duo"
  BDSM → "BDSM & Bondage"
  WS giving → "Water Sports"
  Rimming giving → "Rimming"

slug = name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')

──────────────────────────────────────────────────────────────────
2.3 Districts (25 районов с данными)
──────────────────────────────────────────────────────────────────

Tier 1 (top demand):
Mayfair, Knightsbridge, Chelsea, Kensington, South Kensington,
Belgravia, Notting Hill, Sloane Square, Marylebone, Soho

Tier 2 (high demand):
Victoria, Westminster, Covent Garden, Paddington, Bayswater,
Earls Court, Gloucester Road, Fulham, Hyde Park, Marble Arch

Tier 3 (growing):
Canary Wharf, Shoreditch, Battersea, Maida Vale, St Johns Wood

Для каждого района заполнить:
  seoTitle = "Escorts in {name} | Virel London"
  hotels[] — 3-5 топ отелей района
  restaurants[] — 3-5 топ ресторанов
  landmarks[] — 2-3 достопримечательности

──────────────────────────────────────────────────────────────────
2.4 TransportHubs (20 станций)
──────────────────────────────────────────────────────────────────
Mayfair      → Bond Street (3 min), Green Park (4 min), Oxford Circus (6 min)
Knightsbridge → Hyde Park Corner (2 min), Knightsbridge (3 min)
Chelsea      → Sloane Square (2 min), South Kensington (4 min)
Soho         → Leicester Square (3 min), Piccadilly Circus (4 min)
Victoria     → Victoria (3 min), Pimlico (5 min)
Paddington   → Paddington (2 min), Royal Oak (4 min)
Canary Wharf → Canary Wharf (3 min), Heron Quays (4 min)
Shoreditch   → Liverpool Street (5 min), Aldgate (6 min)

Добавить в package.json:
  "seed:masters": "npx tsx scripts/seed-masters.ts"

Запустить: npm run seed:masters

═══════════════════════════════════════════════════════════════════
ЧАСТЬ 3 — API ROUTES (ADMIN)
═══════════════════════════════════════════════════════════════════

Все admin API: requirePermission + logAudit на мутации.

──────────────────────────────────────────────────────────────────
3.1 Services CRUD
──────────────────────────────────────────────────────────────────
GET    /api/v1/services          → список (фильтры: category, isPublic, isActive)
POST   /api/v1/services          → создать
GET    /api/v1/services/[id]     → один сервис
PUT    /api/v1/services/[id]     → обновить
DELETE /api/v1/services/[id]     → удалить

POST   /api/v1/services/[id]/generate-seo → AI генерация SEO полей
  Вход: { name, category, publicName }
  Промпт:
    "You are an SEO expert for Virel, a luxury companion agency in London.
     Generate sophisticated, tasteful, SEO-optimized content for: {name}
     Public name: {publicName}. Category: {category}.
     Return ONLY valid JSON:
     {
       seoTitle: string (max 60 chars, include 'London'),
       seoDescription: string (max 155 chars),
       seoKeywords: string (5-8 keywords),
       introText: string (80-100 words, sophisticated tone),
       fullDescription: string (400-500 words, SEO-rich, never explicit)
     }"

──────────────────────────────────────────────────────────────────
3.2 Call Rates CRUD
──────────────────────────────────────────────────────────────────
GET    /api/v1/call-rates        → список
POST   /api/v1/call-rates        → создать
PUT    /api/v1/call-rates/[id]   → обновить
DELETE /api/v1/call-rates/[id]   → удалить

──────────────────────────────────────────────────────────────────
3.3 Districts CRUD
──────────────────────────────────────────────────────────────────
GET    /api/v1/districts         → список (с modelCount)
POST   /api/v1/districts         → создать
GET    /api/v1/districts/[id]    → один район + хабы + модели
PUT    /api/v1/districts/[id]    → обновить
DELETE /api/v1/districts/[id]    → удалить

POST   /api/v1/districts/[id]/generate-seo → AI генерация
  Промпт:
    "Write a sophisticated, SEO-optimized description for escorts in {name}, London.
     Include: atmosphere, notable hotels ({hotels}), restaurants ({restaurants}),
     landmarks ({landmarks}), transport links.
     300-400 words. Tasteful, luxury tone. Never explicit.
     Return ONLY valid JSON:
     { seoTitle, seoDescription, seoKeywords, description }"

GET    /api/v1/districts/[id]/transport-hubs → хабы района
POST   /api/v1/districts/[id]/transport-hubs → добавить хаб

──────────────────────────────────────────────────────────────────
3.4 Model Services & Rates & Locations
──────────────────────────────────────────────────────────────────
GET    /api/v1/models/[id]/services   → сервисы модели
POST   /api/v1/models/[id]/services   → { serviceIds, extras: [{serviceId, extraPrice}] }

GET    /api/v1/models/[id]/rates      → тарифы модели
POST   /api/v1/models/[id]/rates      → { rates: [{callRateMasterId, incallPrice, outcallPrice}] }
POST   /api/v1/models/[id]/copy-rates → { fromModelId }

GET    /api/v1/models/[id]/locations  → районы модели
POST   /api/v1/models/[id]/locations  → { districtIds, primaryDistrictId, transportHubId }

POST   /api/v1/models/[id]/generate-bio → AI генерация bio
  Вход: { name, nationality, age, height, tagline, languages, education }
  Промпт:
    "Write a sophisticated, glamorous companion profile bio for {name}.
     {nationality}, {age} years old. {tagline}.
     Languages: {languages}. {education}.
     150-200 words. Warm, intriguing, luxury tone. Never explicit.
     Return ONLY valid JSON: { bio: string, tagline: string }"

POST   /api/v1/models/[id]/verify-photos → AI верификация фото
  Вход: { profilePhotoUrl, selfiePhotoUrl }
  Проверить что на обоих фото одна и та же девушка
  Вернуть: { isVerified: boolean, confidence: number, note: string }
  Установить: model.isVerified = true если прошла

POST   /api/v1/models/quick-upload → AI Quick Upload
  Вход: multipart/form-data { photos[], anketa?: file }
  Процесс:
    1. Сохранить фото в R2
    2. Если есть PDF/DOCX анкета → распарсить через Claude API
    3. AI анализирует фото → определяет hair color, body type
    4. Заполнить все поля модели автоматически
    5. Сгенерировать bio + tagline + slug
    6. Вернуть pre-filled форму для проверки вебмастером
  Создать модель со статусом Draft — вебмастер проверяет и публикует

──────────────────────────────────────────────────────────────────
3.5 Availability
──────────────────────────────────────────────────────────────────
GET    /api/v1/models/[id]/availability       → слоты на неделю вперёд
POST   /api/v1/models/[id]/availability       → установить слот
DELETE /api/v1/models/[id]/availability/[id]  → удалить слот

──────────────────────────────────────────────────────────────────
3.6 Booking Requests
──────────────────────────────────────────────────────────────────
GET    /api/v1/booking-requests          → список (фильтры: status, date)
POST   /api/v1/booking-requests          → создать (из публичного фронта)
GET    /api/v1/booking-requests/[id]     → один запрос
PUT    /api/v1/booking-requests/[id]     → обновить статус/заметки
POST   /api/v1/booking-requests/[id]/confirm → подтвердить бронирование
  → отправить confirmation email клиенту
  → отправить Telegram уведомление рецепции

──────────────────────────────────────────────────────────────────
3.7 Smart Match
──────────────────────────────────────────────────────────────────
POST   /api/v1/smart-match → AI подбор моделей
  Вход: {
    experience: "gfe"|"intimate"|"wellness"|"fetish",
    appearance: "blonde"|"brunette"|"redhead"|"any",
    nationality: string | "any",
    budget: number,
    district: string | "any",
    duration: number,
    occasion: string
  }
  Логика: найти модели которые соответствуют всем параметрам
  Вернуть: топ 3-5 моделей с объяснением почему они подходят
  AI добавляет персональное сообщение для каждой рекомендации

──────────────────────────────────────────────────────────────────
3.8 Concierge Partners
──────────────────────────────────────────────────────────────────
GET    /api/v1/concierge          → список (фильтры: type, district)
POST   /api/v1/concierge          → создать партнёра
PUT    /api/v1/concierge/[id]     → обновить
DELETE /api/v1/concierge/[id]     → удалить

──────────────────────────────────────────────────────────────────
3.9 Blog
──────────────────────────────────────────────────────────────────
GET    /api/v1/blog               → список постов
POST   /api/v1/blog               → создать пост
GET    /api/v1/blog/[id]          → один пост
PUT    /api/v1/blog/[id]          → обновить
DELETE /api/v1/blog/[id]          → удалить
POST   /api/v1/blog/[id]/generate → AI генерация контента блога
  Вход: { title, category, relatedDistricts, relatedServices }
  Вернуть: полный пост 600-800 слов для проверки редактором

──────────────────────────────────────────────────────────────────
3.10 Public API (для фронта — без auth)
──────────────────────────────────────────────────────────────────
GET /api/public/services           → isActive + isPublic сервисы
GET /api/public/services/[slug]    → сервис + модели
GET /api/public/districts          → isActive районы
GET /api/public/districts/[slug]   → район + хабы + модели в районе
GET /api/public/models             → публичный каталог (фильтры)
GET /api/public/models/[slug]      → публичный профиль модели
GET /api/public/concierge          → партнёры для фронта
GET /api/public/blog               → опубликованные посты
GET /api/public/blog/[slug]        → один пост
POST /api/public/smart-match       → AI подбор (анонимный)
POST /api/public/booking-request   → создать запрос на бронирование

═══════════════════════════════════════════════════════════════════
ЧАСТЬ 4 — ADMIN UI (бэк-офис)
═══════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────────────
4.1 Sidebar — обновить навигацию
──────────────────────────────────────────────────────────────────
Добавить разделы:

  Masters
    ├── Services          /admin/masters/services
    ├── Call Rates        /admin/masters/call-rates
    └── Locations         /admin/masters/locations

  Bookings (обновить существующий)
    ├── Booking Requests  /admin/booking-requests  ← новый
    └── Bookings          /admin/bookings

  Concierge              /admin/concierge          ← новый
  Blog                   /admin/blog               ← новый

──────────────────────────────────────────────────────────────────
4.2 /admin/masters/services
──────────────────────────────────────────────────────────────────
Фильтры: Category | Public/Private | Status | Popular | Search
Таблица: Name | Public Name | Category | Public | Popular | Sort | Actions
Actions: Edit | ✨ Generate SEO | Toggle Active | Delete

Модальное окно Add/Edit — 2 вкладки:
  "Basic":
    Title* (text)
    Public Name (text — элегантное название для фронта)
    Slug (auto-generated, редактируемый)
    Category* (select)
    Description (textarea — краткое)
    Is Public (toggle — если off: только Members)
    Is Popular (toggle)
    Is Active (toggle)
    Sort Order (number)

  "SEO":
    SEO Title · SEO Description · SEO Keywords
    Introduction Text (textarea)
    Full Description (textarea)
    Кнопка "✨ Generate with AI" →
      показывает preview →
      "Apply All" заполняет все поля →
      Save

──────────────────────────────────────────────────────────────────
4.3 /admin/masters/call-rates
──────────────────────────────────────────────────────────────────
Таблица: Label | Duration | Sort | Active | Actions
Модальное окно: Label* | Duration (min)* | Sort Order | Active

──────────────────────────────────────────────────────────────────
4.4 /admin/masters/locations
──────────────────────────────────────────────────────────────────
Два таба: Districts | Transport Hubs

Districts таб:
  Таблица: Name | Tier | Models | Popular | Active | Actions
  Actions: Edit | ✨ Generate SEO | Delete
  Модальное окно Edit:
    Name* | Slug (auto) | Tier (select 1/2/3)
    Is Popular | Is Active | Sort Order
    Hotels (tag input) | Restaurants (tag input) | Landmarks (tag input)
    Description (textarea)
    SEO Title | SEO Description | SEO Keywords
    Кнопка "✨ Generate SEO with AI"

Transport Hubs таб:
  Таблица: Name | District | Walking Min | Active | Actions
  Модальное окно: Name* | Slug (auto) | District (select)* | Walking Minutes | Active

──────────────────────────────────────────────────────────────────
4.5 /admin/models/[id] — обновить страницу модели
──────────────────────────────────────────────────────────────────
Добавить секции:

СЕКЦИЯ "Physical Stats":
  Weight (select 40-89 кг)
  Bust Size (select: 28AA→42DD)
  Bust Type (Natural/Enhanced)
  Eye Color (select)
  Measurements (text "32D-25-33")
  Smoking (select)
  Tattoo (select)
  Piercing (multi-select)
  Orientation (select)
  Languages (multi-select)
  Education (text)
  Travel (select)

СЕКЦИЯ "Marketing & Presentation":
  Tagline (text — "Slender Russian Blonde")
  Public Tags (multi-select из предустановленных:
    "Girlfriend Experience" "Open-minded" "Dinner Companion"
    "Travel Partner" "Wellness Specialist" "Fetish-friendly"
    "Duo Available" "Dominatrix" "Massage Specialist")
  Availability (select: Available Now/Advanced Notice/Away/On Holiday)
  Is Exclusive (toggle)
  Is Verified (toggle — после AI верификации)
  Response Time (text — "Usually within 15 min")
  DUO Partners (multi-select из активных моделей)
  SEO Title | SEO Description

СЕКЦИЯ "Wardrobe":
  Multi-select: Schoolgirl | Secretary | Nurse | Bikini | Catsuit
  PVC | Lingerie | Corset | Latex | Stilettos | Bunny
  Stockings | Police | French Maid | Teacher | Stewardess | Housekeeper

СЕКЦИЯ "Locations":
  Multi-select районов из Districts
  Primary Location (radio)
  Nearest Station (select из TransportHubs)
  Walking Minutes (number)

СЕКЦИЯ "Services":
  Сетка чекбоксов по категориям:
  [Signature] [Intimate] [Wellness] [Fetish] [Bespoke]
  Для отмеченных: "Extra service" toggle + "Price £ ___"
  Кнопка "Save Services"

СЕКЦИЯ "Rates":
  Таблица: Duration | Incall £ | Outcall £
  Один ряд для каждого CallRateMaster
  Кнопка "Copy from model" (dropdown)
  Кнопка "Save Rates"

СЕКЦИЯ "Availability":
  Недельный календарь
  Клик на день → установить Available/Busy/Away
  Диапазон времени From/To
  Сохраняется в AvailabilitySlot

СЕКЦИЯ "AI Tools":
  "✨ Generate Bio with AI" → показывает bio + tagline → Apply → Edit → Save
  "✨ Verify Photos with AI" → сравнивает selfie с профильными → Verified badge
  "✨ Generate SEO" → title + description

──────────────────────────────────────────────────────────────────
4.6 /admin/booking-requests ← НОВАЯ СТРАНИЦА
──────────────────────────────────────────────────────────────────
Это рабочий стол рецепции.

Карточки вверху:
  Pending (красный) | Confirmed today | Total this week | Revenue

Таблица:
  Time | Client | Model | Duration | District | Type | Total | Status | Actions
  Actions: Confirm | Reject | View Details | Add Note

Боковая панель (drawer при клике):
  Все детали запроса
  История клиента (предыдущие бронирования)
  Заметки рецепции
  Кнопки: Confirm | Send WhatsApp | Send Telegram | Reject

Фильтры: Status | Date | Model | District
Auto-refresh каждые 30 секунд.

──────────────────────────────────────────────────────────────────
4.7 /admin/concierge ← НОВАЯ СТРАНИЦА
──────────────────────────────────────────────────────────────────
Таблица: Name | Type | District | Price Range | Active | Actions
Типы: Restaurant | Hotel | Chauffeur | Flowers | Gifts | Events
Модальное окно: все поля ConciergePartner
Фильтры: Type | District | Active

──────────────────────────────────────────────────────────────────
4.8 /admin/blog ← НОВАЯ СТРАНИЦА
──────────────────────────────────────────────────────────────────
Таблица: Title | Category | Status | Published | Actions
Actions: Edit | ✨ Generate | Preview | Publish | Delete

Редактор поста:
  Title* | Slug (auto) | Category* (select)
  Cover Image (upload)
  Excerpt (textarea)
  Content (rich text editor)
  Related Districts (multi-select) | Related Services (multi-select)
  Tags (tag input)
  SEO Title | SEO Description
  Is Published (toggle)

Кнопка "✨ Generate Content with AI":
  Вход: title + category + related topics
  AI пишет полный пост 600-800 слов
  Редактор видит результат → правит → публикует

═══════════════════════════════════════════════════════════════════
ЧАСТЬ 5 — ПУБЛИЧНЫЙ ФРОНТ
═══════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────────────
5.1 Общие принципы фронта
──────────────────────────────────────────────────────────────────
- Mobile-first дизайн
- Тёмная тема по умолчанию (люкс стандарт)
- WebP фото с lazy loading
- Panic button в header — мгновенно переключает на нейтральную страницу
- "Virel Concierge" в title браузера — не "escort agency"
- GDPR cookie banner с Privacy-first опцией по умолчанию
- Все бронирования через агентство — модель никогда напрямую

──────────────────────────────────────────────────────────────────
5.2 Главная страница /
──────────────────────────────────────────────────────────────────
Существующая страница — добавить/улучшить:

Hero section:
  Заголовок: "London's Premier Companion Agency"
  Подзаголовок: элегантный, дискретный
  CTA кнопки: "Browse Companions" | "Find Your Match" (→ Smart Match)
  Фото: featured models (из БД, is_exclusive=true)

Featured Companions (динамически из БД):
  Карточки: фото | имя | tagline | район | цена от £X
  Verified badge если isVerified=true
  Available Now badge если availability="Available Now"

Smart Match блок:
  "Not sure where to start?"
  Кнопка "Find Your Perfect Match" → квиз

Services preview:
  4 категории: Companion Services | Wellness | Fetish & Fantasy | Concierge
  Каждая → /services/[category-slug]

London Districts:
  Карточки топ-10 районов → /london/[district]-escorts/

Testimonials (отзывы об агентстве, не о моделях)

──────────────────────────────────────────────────────────────────
5.3 Каталог /companions (или /london-escorts)
──────────────────────────────────────────────────────────────────
Улучшить существующую страницу:

Sidebar фильтры:
  По цене (£300-£500, £500-£800, £800-£1200, £1200+)
  По типу: Blonde | Brunette | Redhead | Black
  По National: British | Russian | Brazilian | Italian | Ukrainian | Other
  По сервису: GFE | Wellness | Fetish | DUO | Travel
  По локации: список Districts
  По доступности: Available Now | Today | This Week
  По возрасту: 18-24 | 25-30 | 30+

Карточки моделей:
  Фото (swipe если несколько)
  Имя + Tagline ("Slender Russian Blonde")
  Verified badge | Available Now badge | Exclusive badge | New badge
  Район + Nearest Station
  Public Tags: "GFE · Open-minded · Duo"
  Цена от £X/hr
  Кнопка "View Profile"

Сортировка: Recommended | Newest | Price Low-High | Price High-Low
Pagination или infinite scroll

──────────────────────────────────────────────────────────────────
5.4 Профиль модели /companions/[slug]
──────────────────────────────────────────────────────────────────
Люкс профиль:

Gallery:
  Главное фото + галерея (swipe/slider)
  Verified photos badge
  Video если есть

Левая колонка — фото + booking widget:
  Цены Incall/Outcall по длительностям
  "Book Now" кнопка → форма бронирования (шаг 1 из 3)
  "Enquire via WhatsApp" кнопка
  "Enquire via Telegram" кнопка
  Available: зелёный индикатор если Available Now

Правая колонка — информация:
  Имя + Tagline + Exclusive badge
  Bio (150-200 слов, элегантный)
  Stats:
    Age | Height | Measurements | Bust | Eye Color
    Nationality | Languages | Education | Travel
  Location:
    Primary District → /london/[district]-escorts/
    Nearest station: "Bond Street — 5 min walk"
  Public Tags (элегантные таги):
    "Girlfriend Experience · Open-minded · Dinner Companion"
  Companion Services (публичные):
    Список сервисов из Signature + Wellness + Fetish
  Extras (видны всем):
    CIM +£70 · A-Level +£150 · WS +£100
  Intimate Services (только после email верификации):
    "Sign in to view full service menu"
  Wardrobe:
    "School Girl · Nurse · Latex · French Maid..."
  DUO Available:
    Карточки DUO партнёров → их профили

Отзывы (reviews) внизу страницы

Schema.org: Person + LocalBusiness
generateMetadata: seoTitle, seoDescription, canonical, OG tags
generateStaticParams: все isActive модели

──────────────────────────────────────────────────────────────────
5.5 Booking Flow (3 шага)
──────────────────────────────────────────────────────────────────
Модальное окно или отдельная страница /book:

Шаг 1 — "Select Your Experience":
  Model (pre-filled если с профиля) + опционально Second Model (DUO)
  Call Type: Incall | Outcall
  Duration (из CallRateMasters)
  Date + Time (с учётом availability модели)
  District или Address для Outcall
  Hotel name + Room number (если hotel)
  Occasion: Birthday | Anniversary | Business | Leisure | Other
  Special Requests (textarea)
  Extras выбор (checkbox с ценами)
  Real-time расчёт суммы:
    Base: £400 · CIM: +£70 · A-Level: +£150 = Total: £620
  Валюта: GBP | EUR | USD | AED

Шаг 2 — "Your Details":
  Name* | Email* | Phone*
  Preferred Contact: WhatsApp | Telegram | Phone | Email
  Telegram username (если Telegram)

Шаг 3 — "Confirm":
  Резюме бронирования
  Disclaimer: "All bookings are for companionship only"
  Кнопка "Submit Request"
  → Создаётся BookingRequest со статусом pending
  → Email клиенту: "We've received your request"
  → Telegram/WhatsApp уведомление рецепции
  → Рецепция подтверждает вручную

──────────────────────────────────────────────────────────────────
5.6 Smart Match /find-your-match
──────────────────────────────────────────────────────────────────
Квиз на 5 вопросов (элегантный UI):

Q1: "What kind of experience are you looking for?"
    → Romantic Connection | Sensual Adventure | Relaxation & Wellness | Fetish & Fantasy

Q2: "Do you have an appearance preference?"
    → Blonde | Brunette | Redhead | Dark Hair | No preference

Q3: "Where would you like to meet?"
    → Select district или "Flexible"

Q4: "How long would you like to spend together?"
    → 1 Hour | 2 Hours | 3 Hours | Overnight

Q5: "What is your budget per hour?"
    → Under £400 | £400-£700 | £700-£1200 | £1200+

→ AI анализирует ответы → показывает 3-5 подходящих моделей
→ Для каждой: персональное объяснение "Why she's perfect for you"
→ CTA: "Book Now" | "View Profile" | "Refine Results"

──────────────────────────────────────────────────────────────────
5.7 Services /services
──────────────────────────────────────────────────────────────────
Каталог всех публичных сервисов:
  Сетка карточек по категориям
  Каждая карточка: название, description, → /services/[slug]

──────────────────────────────────────────────────────────────────
5.8 Service page /services/[slug]
──────────────────────────────────────────────────────────────────
Структура (как Muse GFE страница):
  H1: "{publicName} London | Virel"
  Introduction text
  Сетка моделей с этим сервисом
  Full SEO description
  Related services
  CTA: "Find Your {publicName} Companion"
  Breadcrumb: Home → Services → {publicName}
generateMetadata + generateStaticParams

──────────────────────────────────────────────────────────────────
5.9 District pages /london/[district]-escorts/
──────────────────────────────────────────────────────────────────
Структура (Babylon Mayfair best practice + люкс):
  H1: "Escorts in {name}"
  Intro paragraph (из БД — уникальный)
  Сетка моделей в этом районе
  Transport Hubs блок:
    "Nearby stations: Bond Street (3 min) · Green Park (4 min)"
  Concierge recommendations:
    "Recommended Hotels" | "Fine Dining" — из ConciergePartner
  Соседние районы (internal linking)
  Full description (из БД — уникальный)
  FAQ секция (AI-generated)
  Breadcrumb: Home → London → {name}
  Schema.org: LocalBusiness + BreadcrumbList
generateMetadata + generateStaticParams

──────────────────────────────────────────────────────────────────
5.10 Transport Hub /london/[district]-escorts/[station]-station/
──────────────────────────────────────────────────────────────────
  H1: "Escorts near {station} Station, London"
  Description 150-250 слов
  Сетка моделей из родительского района
  → ссылка на районный хаб
  noindex если description < 150 слов
generateStaticParams + canonical

──────────────────────────────────────────────────────────────────
5.11 Concierge /concierge
──────────────────────────────────────────────────────────────────
Новая страница — уникальная в нише:
  "Complete Your Evening" — люкс позиционирование
  Разделы:
    Fine Dining — рестораны по районам
    Luxury Hotels — escort-friendly отели
    Chauffeur Service — партнёры по транспорту
    Special Occasions — организация вечеров
  Каждая карточка: фото | название | район | ссылка | описание

──────────────────────────────────────────────────────────────────
5.12 Blog /blog
──────────────────────────────────────────────────────────────────
Editorial luxury magazine стиль:
  Hero post | Grid остальных
  Категории: London Guide | Lifestyle | Occasions | Agency News
  Каждый пост связан с районами и сервисами (internal linking)
  SEO: title, description, og tags, schema Article

──────────────────────────────────────────────────────────────────
5.13 Members Area /members (заготовка для Фазы 4)
──────────────────────────────────────────────────────────────────
Landing page: "Exclusive Access for Verified Gentlemen"
  Benefits:
    ✓ Full service menus on every profile
    ✓ Priority booking
    ✓ Exclusive companion content
    ✓ Personal booking history & wishlist
    ✓ Members-only special offers
  Форма регистрации → email верификация → ручное одобрение admin
  После логина: full service menus становятся видны

──────────────────────────────────────────────────────────────────
5.14 Sitemap /sitemap.xml (автогенерация)
──────────────────────────────────────────────────────────────────
src/app/sitemap.ts — включить:
  / (главная)
  /companions
  /services
  /services/[slug] — все isActive
  /london/[district]-escorts/ — все isActive
  /london/[district]-escorts/[station]/ — все isActive
  /concierge
  /blog
  /blog/[slug] — все published
  /companions/[slug] — все isActive модели

═══════════════════════════════════════════════════════════════════
ЧАСТЬ 6 — TRUST & SAFETY СИСТЕМА
═══════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────────────
6.1 Panic Button
──────────────────────────────────────────────────────────────────
В header: незаметная иконка (например маленький X или Esc)
При нажатии: мгновенный redirect на нейтральный URL
  → London Travel & Lifestyle Guide (placeholder страница)
  → или просто закрыть вкладку + clear history

──────────────────────────────────────────────────────────────────
6.2 Photo Verification Badge
──────────────────────────────────────────────────────────────────
На каждом профиле:
  ✓ Verified Photos — если isVerified=true (AI прошёл верификацию)
  ✓ Genuine Photos — стандартный для всех
  ✓ Active Today — если lastActiveAt сегодня

──────────────────────────────────────────────────────────────────
6.3 Response Time Indicator
──────────────────────────────────────────────────────────────────
На профиле агентства и на booking форме:
  "Our team typically responds within 15 minutes"
  (обновляется вручную в настройках)

──────────────────────────────────────────────────────────────────
6.4 Disclaimer (юридическая защита)
──────────────────────────────────────────────────────────────────
Footer + booking форма:
  "All services are for companionship only. Any activities
   between consenting adults are a matter of personal choice."
Age verification: checkbox + IP logging

═══════════════════════════════════════════════════════════════════
ЧАСТЬ 7 — EMAIL & NOTIFICATIONS СИСТЕМА
═══════════════════════════════════════════════════════════════════

Используем src/lib/email.ts (уже создан с Resend)
Добавить шаблоны:

  booking_received     → клиенту после отправки запроса
  booking_confirmed    → клиенту когда рецепция подтвердила
  booking_reminder     → за 2 часа до встречи
  booking_cancelled    → если отменено
  welcome_member       → новому члену Members
  model_returns        → wishlist уведомление когда модель вернулась

Telegram уведомления (через существующий src/lib/telegram/):
  → Рецепции: новый BookingRequest
  → Рецепции: напоминание за 30 минут до встречи
  → Tommy (@Tommy_Fridman): критические алерты

═══════════════════════════════════════════════════════════════════
ЧАСТЬ 8 — ANALYTICS & CRM
═══════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────────────
8.1 Reception Dashboard обновить
──────────────────────────────────────────────────────────────────
Добавить виджеты:
  Clients online now (просматривают сайт)
  Top viewed models today
  Pending booking requests
  Confirmations needed

──────────────────────────────────────────────────────────────────
8.2 Client CRM (в /admin/clients обновить)
──────────────────────────────────────────────────────────────────
Для каждого клиента видны:
  Total spend | Visit count | Last visit
  Preferred models | Preferred districts
  Internal notes рецепции
  Wishlist моделей
  Quick re-book кнопка

──────────────────────────────────────────────────────────────────
8.3 Model Performance
──────────────────────────────────────────────────────────────────
В /admin/models — добавить колонки:
  Views this week | Bookings this month | Conversion rate
  (views → booking request)

═══════════════════════════════════════════════════════════════════
ЧАСТЬ 9 — SEO ТЕХНИЧЕСКАЯ НАСТРОЙКА
═══════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────────────
9.1 Metadata на каждой странице
──────────────────────────────────────────────────────────────────
generateMetadata() на всех динамических страницах:
  title | description | canonical | og:title | og:description
  og:image (фото модели или района) | og:type | robots

──────────────────────────────────────────────────────────────────
9.2 Schema.org разметка
──────────────────────────────────────────────────────────────────
  Главная → LocalBusiness + Organization
  Профиль модели → Person
  Районы → LocalBusiness + BreadcrumbList
  Сервисы → Service + BreadcrumbList
  Блог → Article + BreadcrumbList

──────────────────────────────────────────────────────────────────
9.3 robots.txt обновить
──────────────────────────────────────────────────────────────────
Allow: все публичные страницы
Disallow: /admin/ /api/ /members/ /book/
Noindex: transport hub страницы без контента

──────────────────────────────────────────────────────────────────
9.4 Performance
──────────────────────────────────────────────────────────────────
Все фото: WebP + lazy loading + blur placeholder
generateStaticParams на всех SEO страницах
ISR (Incremental Static Regeneration) для каталога

═══════════════════════════════════════════════════════════════════
ЧАСТЬ 10 — ПОРЯДОК ВЫПОЛНЕНИЯ
═══════════════════════════════════════════════════════════════════

ШАГ 1 — Schema + Seed
  prisma/schema.prisma → добавить все новые модели
  npx prisma db push
  npm run seed:masters

ШАГ 2 — Admin API
  Services CRUD + generate-seo
  Call Rates CRUD
  Districts CRUD + generate-seo + transport-hubs
  Model services/rates/locations/availability
  Model generate-bio + verify-photos + quick-upload
  Booking Requests CRUD + confirm
  Smart Match API
  Concierge Partners CRUD
  Blog CRUD + generate

ШАГ 3 — Admin UI
  Sidebar обновить
  /admin/masters/services
  /admin/masters/call-rates
  /admin/masters/locations
  /admin/models/[id] — все новые секции
  /admin/booking-requests ← рабочий стол рецепции
  /admin/concierge
  /admin/blog

ШАГ 4 — Public API
  Все /api/public/* routes

ШАГ 5 — Public Front
  Главная — улучшить
  /companions — каталог с фильтрами
  /companions/[slug] — профиль модели
  /book — booking flow 3 шага
  /find-your-match — Smart Match квиз
  /services + /services/[slug]
  /london/[district]-escorts/
  /london/[district]-escorts/[station]/
  /concierge
  /blog + /blog/[slug]
  /members (landing page)
  sitemap.xml

ШАГ 6 — Notifications
  Email шаблоны (Resend)
  Telegram уведомления рецепции

ШАГ 7 — SEO Technical
  Metadata на всех страницах
  Schema.org разметка
  robots.txt
  Performance (WebP, ISR)

ШАГ 8 — Trust & Safety
  Panic button
  Photo verification badges
  Disclaimer система
  Age verification

ШАГ 9 — Analytics
  Reception dashboard виджеты
  Client CRM обновить
  Model performance метрики

ШАГ 10 — QA + Deploy
  npx tsc --noEmit (0 errors)
  npx next build (успешный)
  git push → Vercel deploy

═══════════════════════════════════════════════════════════════════
ПРАВИЛА ДЛЯ ВСЕГО КОДА
═══════════════════════════════════════════════════════════════════
- Все admin API: requirePermission + logAudit
- Публичные API: без auth, только isActive/isPublic данные
- AI endpoints: graceful fallback если нет ANTHROPIC_API_KEY
- SEO страницы: generateMetadata() + generateStaticParams()
- Transport hub страницы: noindex если description < 150 слов
- Canonical теги на всех SEO страницах
- Никогда не показывать intimate сервисы без верификации
- Агентство всегда посредник — прямые контакты моделей нигде
- Все бронирования создают BookingRequest → рецепция подтверждает
- После завершения каждого шага: git push + список файлов
═══════════════════════════════════════════════════════════════════