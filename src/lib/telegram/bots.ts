// Stub — real implementation pending
const noop = async (..._args: any[]) => {};

export const divaBot = {
  notifyNewBooking: noop,
  sendMessage: noop,
};

export const keshaBot = {
  syncWithAppSheet: noop,
  sendMessage: noop,
};
